{
  "name": "test-shopify-minicart",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:sass": "sass --watch src/scss:assets/",
    "build:sass": "sass --style=compressed src/scss:assets/",
    "build:js": "terser src/js/main.js --compress --source-map --output assets/main.js"
  },
  "author": "Wesley Soares",
  "license": "ISC",
    "devDependencies": {
    "crossenv": "^0.0.2-security",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "sass": "^1.50.1",
    "terser": "^5.12.1"
  }
}
